<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/viewButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="View" />

        <Button
            android:id="@+id/scrollButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ScrollView" />

        <Button
            android:id="@+id/listButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ListView" />

        <Button
            android:id="@+id/gridButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="GridView" />

        <Button
            android:id="@+id/recyclerButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="RecyclerView" />

        <Button
            android:id="@+id/expandableButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ExpandableListView" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:text="控件：
        \n    1.RefreshViewContainer -> View
        \n    2.RefreshListViewContainerr -> ListView
        \n    3.RefreshGridViewContainerr -> GridView
        \n    4.RefreshExpandableListViewContainerr -> ExpandableListView
        \n    5.RefreshRecyclerViewContainerr -> RecyclerView
        \n实现步骤：
        \n    1.控件2345通过setDefaultRefreshLoadController设置默认Load控制器，也可为每个控件单独设置，见第4步。
        \n    2.布局引用RefreshContainer的实现类，可嵌入对应子控件，如未嵌入，自动创建。
        \n    3.调用setMode()设置刷新模式
        \n    4.控件2345调用setRefreshLoadController()单独设置控制器，如已设置步骤1，可无需此步。
        \n    5.控件2345调用setAdapter() 此前必须确保步骤1或4已设置，注意不能设置给子控件。
        \n    6.调用setOnRefreshListener/setOnLoadListener监听回调
        \n    7.调用startRefreshing/startLoading主动触发控件"
            android:textSize="12dp" />

    </LinearLayout>
</ScrollView>